name = "dual-agent-coordinator-test"
main = "src/worker.ts"
compatibility_date = "2026-09-21"
workers_dev = true

[[durable_objects.bindings]]
name = "PROJECTS"
class_name = "ProjectDurableObject"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["ProjectDurableObject"]

[vars]
# COORDINATOR_API_TOKEN is intentionally not stored here.
# Set it only as a Wrangler secret in the test environment.
# COORDINATOR_EXECUTOR_TOKENS_JSON must also be injected as a Wrangler secret.
