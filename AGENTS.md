# AGENTS.md — 协调系统仓库工程规范

本文件是本仓库对双方 agent 的强制约束。依据《双人 Agent 自动并行开发项目书》第 5 节编写。

> **本文件是说明，不是执行机制。** 第 5 节明确要求：关键限制必须在执行器、仓库权限和 CI 中检查。
> 当本文件与执行器/CI 的实际检查冲突时，以更严格的一方为准。
> 个人已有规则更严格时，不因云端任务而放宽。

---

## 1. 仓库定位与边界

本仓库是**协调系统**（协调服务、协议、执行器、CLI），**不是**业务代码仓库。

- 目标业务代码在独立的目标仓库中，通过 `<TARGET_REPO_URL>` 引用。
- **业务 agent 不得修改本仓库的验收规则或 CI** 来降低自己的验收门槛（第 4.2 节）。
- 协调系统与目标仓库必须分开，避免"自己给自己放行"。

---

## 2. 目录结构与归属

| 路径 | 内容 | 维护者 |
| --- | --- | --- |
| `packages/protocol/` | 协议 v1：schema、状态机、错误分类、样例 | **A（Codex）** |
| `apps/coordinator/` | Worker API 与 Durable Object | **A（Codex）** |
| `apps/executor/` | Windows 执行器公共内核与适配器 | **B（OpenCode）** |
| `tools/` | 管理 CLI、校验工具 | 按文件归属，见下 |
| `tests/` | 单元、协议、故障与端到端测试 | 按文件归属，见下 |
| `docs/` | 接口、授权、操作与验收说明 | 双方共同，改动需在 PR 说明 |
| `.local/` | 非敏感本机运行状态 | 各自本地，**忽略提交** |

**共享文件有明确维护者**（第 P2 节）：

| 文件 / 目录 | 维护者 | 说明 |
| --- | --- | --- |
| `packages/protocol/**` | A | 协议冻结的唯一来源；B 通过变更提案参与 |
| `apps/coordinator/**` | A | B 不得直接修改 |
| `apps/executor/**` | B | A 不得覆盖 |
| `tools/validate-protocol/**` | A | 双方都依赖，改动视为协议改动 |
| `.github/workflows/**` | A | 属第 11 节"CI/CD 配置"，需审核批准 |
| `AGENTS.md` | 双方 | 改动需在 PR 中说明理由 |

### B 端（OpenCode）负责范围

按第 13 节"给 B 端"与 P2 分工表：

- 执行器公共内核：Git/worktree、心跳、进程控制、恢复
- OpenCode 适配器独立模块
- 结果归一化、上下文导出、资料校验
- Windows 与两种适配器兼容性测试

**B 不得**：直接修改 A 的协调器与 CI、重复搭建云端资源、绕过协议提案改字段。

---

## 3. 分支与提交规则

### 3.1 分支命名（规则 1）

每项任务使用独立分支，格式固定：

```
task/<TASK_ID>/<ATTEMPT_ID>
```

例：`task/TASK-0002/TASK-0002-A1`

- 一个尝试一个分支，返修产生新尝试即新分支。
- 禁止在 `main` 上直接开发。
- 禁止复用已合并的任务分支。

### 3.2 工作目录（第 5 节末段）

- 任务工作目录使用目标仓库的**独立 worktree**。
- **不得**在用户当前交互开发目录中自动切分支。
- 已有未提交内容不得被覆盖（第 8 节步骤 2）。

### 3.3 提交要求

- 提交信息首行格式：`<TASK_ID>: <简述>`，正文记录 attempt_id 与依据的契约版本。
- 不得提交敏感文件（见第 5 节）。
- 锁文件必须一并提交，依赖版本锁定（第 4.1 节）。

---

## 4. 版本绑定（规则 2）

**每次领取绑定基线 SHA、规则、契约和验收版本。** 四项缺一不可。

```ts
{
  base_sha:       // 任务开工的 Git 基线
  rules_sha:      // AGENTS.md 所在提交
  contract_sha:   // 接口契约所在提交
  acceptance_sha: // 独立验收基线所在提交
}
```

- 任一为空 → 执行器必须拒绝开工，标记为阻塞并要求介入。
- 任务运行过程中，若上述任一项在远端发生变化 → 当前批次的通过记录失效（验收 V07）。
- 不允许"途中悄悄读取最新版本"。资料与契约只使用绑定哈希的版本（第 10.2 节同原则）。

---

## 5. 修改范围（规则 3）

**agent 只能修改允许范围；实际 Git diff 必须再由执行器检查。**

- 每个任务在规划输出中声明 `write_scope.allow` 与 `write_scope.deny`。
- `deny` 优先级高于 `allow`。
- 执行器在提交前**必须**用 `git diff --name-only <base_sha>..HEAD` 核对实际改动文件：
  - 出现 `allow` 之外的路径 → 拒绝提交，错误码 `DIFF_OUT_OF_SCOPE`。
  - 出现 `deny` 中的路径 → 拒绝提交。
- 声明范围与意图不符时，走协议提案调整规划，**不得**先改代码再补声明。

---

## 6. 验收底线（规则 4）

- **不删除或跳过测试来通过验收。**
- **不修改验收规则为自己放行。**
- 实现任务可以新增必要测试，但**不能放宽独立验收基线**（第 6 节）。
- 验收版本变化走独立流程；不能由失败任务通过删除断言自行解决。
- 涉及验收基线的改动需在 PR 中单独说明，并明确标注为"验收基线变更"。

对应错误码：`ACCEPTANCE_TAMPERED`（终态失败，需人工判断）。

---

## 7. 敏感文件（规则 5）

**不提交、不上传、不进入日志。** 包括但不限于：

- 登录文件、token、私钥、证书（`*.pem` `*.key` `*.p12` `*.pfx` `auth.json`）
- `.env` 及其变体
- agent 本地会话目录（`.codex/`）
- 模型隐藏推理、原始会话全文、整个电脑内容（第 11 节）

规则：

- 登录文件、密码、私钥**不得**交给对方或上传云端（第 4.1 节）。
- 日志与资料在上传前做敏感内容检查（第 11 节）。
- 检测到疑似敏感文件进入 diff → 拒绝提交，错误码 `SENSITIVE_FILE_DETECTED`。

---

## 8. 授权表（规则 6）

下列操作必须获得对应授权。授权记录只保存**非敏感范围与确认依据**；相同有效范围不重复询问；新增高权限操作**不得自动扩展**许可（第 11 节）。

| 操作 | 授权要求 |
| --- | --- |
| 只读检查、文档、允许目录中的代码与测试 | 按项目规则执行，无需额外批准 |
| 任务分支 push | 明确仓库、分支前缀、有效期；可一次授权同一范围 |
| CI/CD 配置 | 审核具体文件和权限后批准 |
| Worker 部署、凭据创建、初始数据库 schema | 明确账号、环境、资源和变更后批准 |
| 自动合并 | 单独明确目标分支、通过门槛和适用范围 |
| 系统安装、自启、删除、迁移、生产发布 | 按准确对象另行批准 |
| 同伴项目通知 | 首次确认接收者与消息范围；可限定项目持续授权 |

**默认立场：未经授权的高权限操作一律不做，并标记 `blocked_approval`。**

其他受限动作对应错误码：`UNAUTHORIZED_OPERATION`、`PUSH_REJECTED`。

---

## 9. 清理规则（规则 7）

**工作目录、日志和资料不自动清理；清理需有明确范围与许可。**

- 不得对工作目录、日志、资料执行自动删除或循环清理。
- 清理必须先给出明确范围（具体路径、数量、时间窗）并获得批准。
- 不得开启"自动清理历史"的默认策略（第 10.3 节）。
- 涉及删除的操作属第 8 节授权表"系统安装、自启、删除、迁移"，需按准确对象另行批准。

### 9.1 Syncthing 相关（已启用）

同步已启用，因此以下两条**必须**遵守（第 10.3 节）：

- **发布目录只新增、不自动清理**：删除源文件会传播到对方，属删除操作，需先批准。
- **禁止点击 Override Changes / Revert Local Changes**：它们可能覆盖或删除文件。

同步资料必须先记录 SHA-256 并核对，**不得**在任务途中悄悄读取"最新资料"。
不完整的资料包不得作为任务输入（验收 V16）。

---

## 10. 验证命令

在仓库根目录执行。**以实际命令输出为证据，不接受 agent 自报成功。**

```bash
npm ci                          # 按锁文件安装（CI 与首次克隆必须用它）
npm run typecheck               # TypeScript 全量类型检查
npm run validate:protocol       # 协议元数据自检 + 全部样例正反向校验
npm test                        # 单元、协议、状态机、图分析测试
npm run check                   # 上述三项串联，提交前必须全绿
```

CI 与本规范使用的命令必须一致。改变验证命令视为改动验收基线，走第 6 节流程。

---

## 11. 并行与串行判定

- **影响同一文件的任务先串行处理**；能拆为独立模块时重新规划（第 6 节）。
- 判定依据：写入范围是否可能重叠 + 是否存在依赖关系。
- 保守原则：无法静态判定时判定为**可能重叠**，宁可串行，不可误并行。
- 同一项目一次仅整合一个批次（第 7 节默认值）。

实现见 `packages/protocol/src/graph.ts` 的 `canRunInParallel` / `globalsMayOverlap`。

---

## 12. 状态机

任务状态转移**只能**走 `packages/protocol/src/status.ts` 中的 `TASK_TRANSITIONS`。

- 正常路径：`draft → planning → ready → leased → running → validating → ready_for_integration → integrating → passed → merged`
- 异常：`repair_pending` / `blocked_auth` / `blocked_quota` / `blocked_approval` / `needs_input` / `cancelled`
- 新增转移必须走协议变更提案（`docs/protocol-changes.md`）。
- 单任务可在自身契约测试通过后进入 `ready_for_integration`，但**完整功能只能由组合测试验收**。

---

## 13. 错误分类纪律（验收 V10）

**登录过期与配额不足必须分别标记，不得按代码失败反复返修。**

`packages/protocol/src/errors.ts` 的 `ERROR_POLICY` 是唯一判定依据：

- `repairable` → 计入返修次数，自动派发返修任务
- `blocked` → 阻塞当前任务等待介入，**不计入**返修次数
- `retryable` → 执行器侧放弃本次尝试，由协调器重派
- `fatal` → 终态失败，需人工判断

返修上限：`DEFAULT_LIMITS.maxRepairAttempts = 2`（第 7 节可调建议值）。

---

## 14. 开工前自检清单

B 端（OpenCode）开工前确认：

- [ ] 已读本文件及适用父级规则
- [ ] 新仓库共同基线 SHA 已与 A 端核对一致
- [ ] 工具版本矩阵已核对，`npm ci` 通过
- [ ] 自己的模型服务登录可用（**不得**假定可复用对方的 Codex 订阅）
- [ ] 协议 v1 已审核，接口冻结与实施计划已确认
- [ ] 已确认自己不修改 A 的协调器与 CI

A 端（Codex）开工前确认：

- [ ] 已读本文件及适用父级规则
- [ ] 项目目标、资源所有者、预算与项目路径已由用户确定
- [ ] 双方协作权限已配置
- [ ] 不覆盖 B 负责的执行器内核
